{
  "name": "data_shifter",
  "private": true,
  "scripts": {
    "prepare": "husky",
    "precommit": "lint-staged --quiet"
  },
  "devDependencies": {
    "husky": "^9.0.0",
    "lint-staged": "^16.0.0"
  }
}
