# internal-docs

Internal, non-shipped notes: brainstorming specs (`specs/`), implementation plans (`plans/`),
and anything the `superpowers` skills generate. Excluded from the packaged gem (`spec.files`).

`docs/` (not yet created) is reserved for a future user-facing site — keep internal notes here.
